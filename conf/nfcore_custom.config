// Empty placeholder — prevents the pipeline from trying to load
// the remote nfcore_custom.config from nf-core/configs.
// We supply the Cheaha config directly via -c conf/cheaha.config instead.
profiles {}
